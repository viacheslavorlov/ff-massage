---
import { Image } from "astro:assets";

const { title, image } = Astro.props;
---

<div
  class="relative flex w-full flex-col items-center rounded-[20px] border border-primary p-6 pt-8"
>
  <p>{title}</p>
  <div
    class="right-[calc(50% - 28px)] px-3 py-3 absolute -top-7 h-14 w-14 rounded-full border border-primary bg-black p-2 lg:-right-7"
  >
    <Image src={image} class="object-contain" width={50} height={50} alt="" />
  </div>
</div>
